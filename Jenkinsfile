pipeline{
    agent any
    stages{
        stage("Build"){
            steps{
                echo "========= Build steps started =========="
                sh 'echo "Build completed"'
                echo "========= Build steps completed ========"
            }
        }
    }
}